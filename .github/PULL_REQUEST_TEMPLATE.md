## What this changes

## Checklist

- [ ] `npm run check` passes and leaves the git tree clean
- [ ] I did not edit any existing immutable raw snapshot (`data/popularity/<cat>/<YYYY-MM>.json`)
- [ ] If I added/changed a category or keyword mapping, I explained the reasoning
- [ ] Docs updated if behavior changed
